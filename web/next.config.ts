import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "i.ibb.co",
      "ui-avatars.com",
      "images.unsplash.com",
      "localhost",
      "example.com",
      "200lab.io",
    ],
  },
};

export default nextConfig;
