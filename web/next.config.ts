import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "i.ibb.co",
      "ui-avatars.com",
      "images.unsplash.com",
      "localhost",
      "example.com",
    ],
  },
};

export default nextConfig;
